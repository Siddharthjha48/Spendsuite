name: Keep Render Awake

on:
  schedule:
    - cron: "*/14 * * * *"   # Every 14 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl request
        run: curl https://spendsuite.onrender.com
